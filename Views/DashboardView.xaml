<Page
    x:Class="SistemaGestaoDeBovinos.Views.DashboardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SistemaGestaoDeBovinos.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:SistemaGestaoDeBovinos.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <viewmodels:DashboardViewModel/>
    </Page.DataContext>

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Bem-vindo ao Sistema de Gestão de Bovinos!" Style="{StaticResource HeaderTextBlockStyle}" Margin="0,0,0,24"/>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Número de Animais:" Style="{StaticResource SubheaderTextBlockStyle}" Margin="0,0,12,0"/>
                <TextBlock Text="{Binding NumeroDeAnimais}" Style="{StaticResource SubheaderTextBlockStyle}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                <TextBlock Text="Número de Lotes:" Style="{StaticResource SubheaderTextBlockStyle}" Margin="0,0,12,0"/>
                <TextBlock Text="{Binding NumeroDeLotes}" Style="{StaticResource SubheaderTextBlockStyle}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                <TextBlock Text="Número de Pastos:" Style="{StaticResource SubheaderTextBlockStyle}" Margin="0,0,12,0"/>
                <TextBlock Text="{Binding NumeroDePastos}" Style="{StaticResource SubheaderTextBlockStyle}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
