<Page
    x:Class="SistemaGestaoDeBovinos.Views.AddEditFinanceiroView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SistemaGestaoDeBovinos.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel Margin="24">
            <TextBox Header="Descrição" Text="{Binding Financeiro.Descricao, Mode=TwoWay}"/>
            <TextBox Header="Valor" Text="{Binding Financeiro.Valor, Mode=TwoWay}" Margin="0,12,0,0"/>
            <DatePicker Header="Data" Date="{Binding Financeiro.Data, Mode=TwoWay}" Margin="0,12,0,0"/>
            <ComboBox Header="Tipo" SelectedItem="{Binding Financeiro.Tipo, Mode=TwoWay}" Margin="0,12,0,0">
                <x:String>Entrada</x:String>
                <x:String>Saída</x:String>
            </ComboBox>
            <StackPanel Orientation="Horizontal" Margin="0,24,0,0">
                <Button Content="Salvar" Command="{Binding SaveCommand}"/>
                <Button Content="Cancelar" Command="{Binding CancelCommand}" Margin="12,0,0,0"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
