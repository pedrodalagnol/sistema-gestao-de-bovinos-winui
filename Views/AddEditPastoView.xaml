<Page
    x:Class="SistemaGestaoDeBovinos.Views.AddEditPastoView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SistemaGestaoDeBovinos.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel Margin="24">
            <TextBox Header="Nome" Text="{Binding Pasto.Nome, Mode=TwoWay}"/>
            <TextBox Header="Área (ha)" Text="{Binding Pasto.AreaHa, Mode=TwoWay}" Margin="0,12,0,0"/>
            <ComboBox Header="Status" 
                      ItemsSource="{Binding StatusOptions}" 
                      SelectedItem="{Binding Pasto.Status, Mode=TwoWay}" 
                      Margin="0,12,0,0"/>
            <DatePicker Header="Data Início Status" Date="{Binding Pasto.DataInicioStatus, Mode=TwoWay}" Margin="0,12,0,0"/>
            <StackPanel Orientation="Horizontal" Margin="0,24,0,0">
                <Button Content="Salvar" Command="{Binding SaveCommand}"/>
                <Button Content="Cancelar" Command="{Binding CancelCommand}" Margin="12,0,0,0"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
